<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d"
             xmlns:local="clr-namespace:ExpressBase.Mobile.CustomControls"
             x:Class="ExpressBase.Mobile.Views.Login">
    <ContentPage.Content>
        <StackLayout Margin="20" VerticalOptions="CenterAndExpand">
            <StackLayout>
                <Image x:Name="Logo" Aspect="AspectFill"></Image>
            </StackLayout>
            <StackLayout Margin="0,5,0,5">
                <Label Text="Username" />
                <local:TextBox x:Name="UserName" Text="{Binding UserName}"/>
            </StackLayout>
            <StackLayout Margin="0,5,0,20">
                <Label Text="Password" />
                <local:TextBox x:Name="PassWord" />
            </StackLayout>
            <Button Text="Login" 
                    BackgroundColor="#508bf9"
                    TextColor="White"
                    HeightRequest="40"
                    Clicked="OnLoginClick" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>